node_modules/

# eval outputs
evals/results/*
!evals/results/.gitkeep
evals/questions.generated.json

# Local investigation scripts — read secrets from the environment, never commit them
measure-requote-timing.js
.DS_Store

# wrangler local state (dev cache, miniflare KV/D1) — never commit
.wrangler/

# Worker secrets for local runs (PRIMUS_USER/PRIMUS_PASS, Stripe keys). Wrangler reads
# these automatically; they must never enter the repo. Was NOT ignored until 2026-08-04.
.dev.vars
**/.dev.vars
